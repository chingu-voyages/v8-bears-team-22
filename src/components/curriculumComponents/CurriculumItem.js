import React from 'react';

const CurriculumItem = (props) => {
    const item = props.item

    // renderItem = () => {
        
    // }

    return (
        <ul>
            <li>{item}</li>
        </ul>
    )
}

export default CurriculumItem;